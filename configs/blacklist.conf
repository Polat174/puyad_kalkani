# PUYAD Kalkanı — Kara Liste
# Güvenlik riski oluşturan paket ve servisler.
# Boş satırlar ve # ile başlayan satırlar yok sayılır.

# Uzaktan erişim — şifresiz protokoller
telnetd
telnet-server
rsh-server
rsh
rlogin
talk
talk-server

# Güvensiz dosya transfer
tftp-server
tftp
vsftpd

# Gereksiz ağ servisleri
xinetd
ypserv
ypbind
rpcbind
avahi-daemon

# Yazdırma (sunucularda gereksiz)
cups

# NFS (gerekli değilse)
nfs-kernel-server
nfs-common

# LDAP (gerekli değilse)
slapd

# SNMP (gerekli değilse)
snmpd

# Samba (gerekli değilse)
samba

# Proxy (gerekli değilse)
squid
